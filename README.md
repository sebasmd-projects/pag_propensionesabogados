# PROPENSIONES

## New project
```cmd
uv init
uv venv
.venv\Scripts\activate
uv add -r requirements.txt
uv run python manage.py runserver
```

## Initialize
```cmd
uv sync
.venv\Scripts\activate
uv run python manage.py runserver
```